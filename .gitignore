/target

# Local runtime state (eframe persistence)
/data
app.ron

# Screenshots that scripts/instrument-test.ps1 leaves behind
/scripts/*.png
# A config backup from before that script ran under its own CHRONODESK_CONFIG;
# it no longer writes one, but a copy may still be lying around.
/scripts/user_app_instr.ron
